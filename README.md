# Ракурс

Кабинет отслеживания продаж по креативам: показывает, какая реклама приносит **деньги**,
а не заявки, и где эти деньги теряются в переписке продавцов.

| Пакет | Что это |
|---|---|
| [`rakurs/`](rakurs/README.md) | фронтенд — React, семь экранов |
| `server/` | бэкенд — Fastify, PostgreSQL |
| `packages/contract/` | доменные типы, общие для обоих: контракт не может разъехаться |
| `deploy/` | Compose, nginx, [инструкция по деплою](deploy/README.md) |

## Запуск локально

Нужен Docker. Первый раз:

```bash
npm install
cp server/.env.example server/.env
docker compose -f deploy/compose.dev.yml up -d
DATABASE_URL=postgres://rakurs:rakurs@localhost:55433/rakurs_dev npm --prefix server run migrate
```

Завести себе пользователя — экрана регистрации нет и не будет, кабинет одной компании:

```bash
npm --prefix server run create-user
```

Дальше два терминала:

```bash
npm --prefix server run dev
```

```bash
npm --prefix rakurs run dev
```

Кабинет на http://localhost:5173, бэкенд на http://localhost:3000. Переменные сервер берёт
из `server/.env` сам, руками ничего передавать не нужно.

База в `compose.dev.yml` лежит на именованном томе, поэтому пользователь и данные переживают
перезапуск контейнера. Тестовая база — отдельная (`compose.test.yml`, порт 55432, tmpfs):
её каждый прогон тестов чистит, и заводить там что-либо бессмысленно.

## Проверки

```bash
npm --prefix server test        # нужна тестовая база: docker compose -f deploy/compose.test.yml up -d
npm --prefix server run typecheck
npm --prefix rakurs run typecheck
npm --prefix rakurs run build
```

## Что уже работает

Вход и выход, сессии, профиль и настройки — на настоящих данных из PostgreSQL.

Остальные экраны показывают «Раздел ещё не подключён»: их эндпоинтов пока нет. Это
ожидаемое состояние, а не поломка — данные для них приходят на следующих этапах.

| Этап | Что подключает | Состояние |
|---|---|---|
| 1 | Каркас, база, авторизация, деплой | готово |
| 2 | Meta Marketing API: объявления, расход, коды атрибуции | дальше |
| 3 | WhatsApp через Baileys: привязка по QR, диалоги | |
| 4 | Разбор диалогов моделью, метрики продавцов | |
| 5 | Импорт оплат, Conversions API, сверка | |

Замысел и решения — в [`docs/superpowers/specs/`](docs/superpowers/specs/2026-08-06-rakurs-production-design.md),
план работ — в [`docs/superpowers/plans/`](docs/superpowers/plans/2026-08-06-foundation.md).

## Правила, которые легко нарушить

**Ничего секретного с префиксом `VITE_`.** Всё с этим префиксом вкомпилируется в бандл и будет
видно любому, кто откроет исходники страницы. Токены Meta и ключ модели живут только на сервере.

**Контракт правится в `packages/contract/`**, а не в двух местах. Фронт и сервер импортируют
один файл, поэтому расхождение — ошибка компиляции, а не сюрприз в проде.

**Ноль вместо «не знаем» — ошибка.** Если данных нет, ответ содержит `null`, а экран пишет
словами, чего именно нет. Выдуманные цифры и статусы недопустимы: на них принимают решения
о деньгах.
