# Independent CRM Completion Plan

**Owner:** Astra specification and coordination; gpt-5.6-sol implementation and verification.

**Spec:** `docs/superpowers/specs/2026-09-13-independent-crm-design.md`

## Task 1: Payment Evidence Guards

**Owner:** semantic_qa. **Files:** `server/src/lib/crm/analysis.ts`, `server/test/crm-analysis.test.ts`, the focused assertion in `server/test/crm-worker.test.ts`.

1. Require real client-authored messages and exact text quotes for fresh text evidence.
2. Replace unsupported attachment-content claims with a deterministic neutral review reason; never imply OCR.
3. Add Russian/Kazakh synthetic regression cases and update the stale profile expectation with explicit payment assertions.
4. Transfer only these focused changes into the authoritative release tree and run unit, worker, and Kaspi tests against a uniquely named isolated database.

## Task 2: Independent Analysis Policy

**Owner:** sol_work. **Files:** existing schema/contract/settings API, `server/src/lib/automation/policy.ts`, `server/src/lib/crm/worker.ts`, Agent screen/settings controls, relevant board status API, and a forward migration after the verified current journal.

1. Add and validate `crmAnalysisMode: 'follow_ai' | 'independent'`, default `follow_ai`, with existing owner authorization.
2. Implement independent CRM eligibility in both policy and worker selection without changing reply eligibility.
3. Enforce zero message/invoice/conversion side effects from independent analysis, including stale live-message/checkout markers.
4. Add the Russian setting and accurate pending/disabled status copy using existing UI patterns.
5. Test legacy behavior, independent mode with replies disabled, tenant access, payment authority, cursor progress, and stale markers. Coordinate edits to worker tests with Task 1's owner.

## Task 3: Release Verification

**Owner:** release_review, after implementation. Review the focused policy and side-effect boundaries independently; verify the new migration against the actual journal without modifying previously applied migrations.

Run relevant tests, both type checks, and production builds once the final candidate is stable. Recheck the responsive settings UI and preserve already verified modal backdrop/mobile/focus behavior. Record actual test counts without double-counting overlapping runs.

## Task 4: Deploy and Activate Sealhouse Analysis

**Owner:** sol_work.

1. Reconcile candidate changes with the current deployed state; preserve unrelated current work.
2. Back up affected state, prepare a complete manifest, and successfully build the candidate before restart.
3. Apply only the required forward migration, publish the matching API/frontend, and record commit/source manifest/image and asset hashes in the release receipt.
4. Enable independent CRM only for Sealhouse, leaving replies/test contact/conversation toggles intact.
5. Start a scoped resumable reanalysis of existing Sealhouse conversations through the proven side-effect-free path. Verify actual analysis results and zero unintended sends/invoices/conversion events using aggregates.
6. Verify health, settings, release identity, and live funnel behavior; report any remaining uncertainty honestly.

## Completion Criteria

- Unsupported payment explanations are rejected or replaced with neutral attachment review text.
- Relevant tests pass, including the previously stale assertion.
- Sealhouse conversations receive CRM analysis while automatic reply settings remain unchanged.
- No customer message, invoice, or conversion event is generated by independent analysis.
- The running release can be identified from recorded image and asset hashes.
