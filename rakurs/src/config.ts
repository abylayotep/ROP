/**
 * Что показывать в кабинете. Это переключатели самого интерфейса, а не данные
 * клиента: всё, что относится к конкретному бизнесу — название, валюта, тариф,
 * цифры — приходит с бэкенда.
 */
export const features = {
  /** Блок сверки CRM → Ракурс → Meta на обзоре. */
  capiPanel: true,
  /** Черновик следующего сообщения в разборе диалога. */
  aiDrafts: true,
};
