/**
 * Контракт с бэкендом живёт в @rakurs/contract — сервер импортирует тот же файл,
 * поэтому формы ответов не могут разъехаться незаметно.
 *
 * `export type *`, а не `export *`: в пакете одни типы, и ключевое слово `type`
 * стирает выражение при сборке вместо того, чтобы тянуть в бандл пакет,
 * у которого нет кода времени выполнения.
 */
export type * from '@rakurs/contract';
